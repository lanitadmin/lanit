<ui:include src="#{controller.getEntityForm(var.id)}">
	<ui:param name="entityHome"
		value="#{controller.entityHomes[var.id]}" />
	<ui:param name="param" value="#{controller.entityHomes[var.id]}" />
</ui:include>
